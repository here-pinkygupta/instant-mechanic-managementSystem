import {Router} from "express"; import * as c from "../controllers/mechanic.controller"; import {requireAuth,requireRoles} from "../middleware/auth.middleware"; import {asyncHandler} from "../utils/asyncHandler"; const r=Router();r.use(requireAuth);r.get("/",requireRoles("ADMIN","OPERATIONS"),asyncHandler(c.list));r.get("/:id",asyncHandler(c.detail));r.patch("/:id",requireRoles("ADMIN","OPERATIONS"),asyncHandler(c.update));export default r;
