import {Router} from "express"; import {list} from "../controllers/service.controller"; import {requireAuth} from "../middleware/auth.middleware"; import {asyncHandler} from "../utils/asyncHandler"; const r=Router();r.get("/",requireAuth,asyncHandler(list));export default r;
